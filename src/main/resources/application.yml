spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://config-server.config-server.svc.cluster.local:80
      name: auth-service
      label: main
      fail-fast: true
  application:
    name: auth-service
  profiles:
    active: prod